<!DOCTYPE html>
<html lang="{{ lang }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="color-scheme" content="light dark">
        <link rel="stylesheet" href="{{ "/assets/style.css" | url }}">
        <link rel="stylesheet" href="{{ "/assets/atom-one-light.css" | url }}" media="screen and (prefers-color-scheme: light)">
        <link rel="stylesheet" href="{{ "/assets/atom-one-dark.css" | url }}" media="screen and (prefers-color-scheme: dark)">
        <title>{{ metas.site }} | {{ title }}</title>
        <meta name="author" content="{{ author.name }}">
        <meta property="article:published_time" content="{{ date | date('ATOM') }}">
        <link rel="canonical" href="{{ url | url(true) }}">
        <link type="application/rss+xml" rel="alternate" href="{{ "/feed.xml" | url(true) }}" title="Sebastian Wiesner">
        <link type="application/json" rel="alternate" href="{{ "/feed.json" | url(true) }}" title="Sebastian Wiesner">
        {% if pageFeed -%}
        <link type="application/rss+xml" rel="alternate" href="{{ pageFeed.rssUrl | url(true) }}" title="{{ pageFeed.title }}">
        <link type="application/json" rel="alternate" href="{{ pageFeed.jsonUrl | url(true) }}" title="{{ pageFeed.title }}">
        {%- endif %}
        {%- if isProduction -%}
        <script data-goatcounter="https://swsnr.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>
        {%- endif -%}
    </head>
    <body>
        <header>
            <nav>
                <ul>
                    <li><strong><a href="{{ "/" | url }}">{{metas.site}}</a></strong></li>
                </ul>
                <ul>
                    {% for link in navLinks %}<li><a href="{{ link.href | url }}">{{ link.text | safe }}</a></li>{% endfor %}
                </ul>
            </nav>
        </header>
        <main>
            {{ content | safe }}
        </main>
        <footer>
            <nav>
                <ul>
                    <li><small>{{metas.siteDescription}}</small></li>
                </ul>
                <ul>
                    <li><a href="mailto:{{author.email}}">Send email to author</a></li>
                    {% if pageFeed -%}
                    <li><a href="{{ pageFeed.rssUrl | url }}">Subscribe to this page</a></li>
                    {%- else -%}
                    <li><a href="{{ "/feed.xml" | url }}">Subscribe</a></li>
                    {%- endif %}
                    <li><a href="https://github.com/swsnr/swsnr.de">Sources of this site</a></li>
                </ul>
            </nav>
        </footer>
  </body>
</html>
