<!DOCTYPE html>
<html lang="{{ lang }}">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="{{ "/style.css" | relative_url }}">
        <!-- TODO: SEO metadata -->
        <!-- TODO: Link to feek -->
        {%- if 'TODO' == 'production' -%}
        <script data-goatcounter="https://swsnr.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>
        {%- endif -%}
    </head>
    <body>
        <header>
            <nav>
                <ul>
                    <li><strong><a href="{{ "/" | relative_url }}">{{siteMeta.title}}</a></strong></li>
                </ul>
                <ul>
                    <li><a href="{{ "/about" | relative_url }}">About</a></li>
                    <li><a href="{{ "/privacy" | relative_url }}">Privacy policy</a></li>
                </ul>
            </nav>
        </header>
        <main>
            {{ content }}
        </main>
        <footer>
            <nav>
                <ul>
                    <li><small>{{siteMeta.description}}</small></li>
                </ul>
                <ul>
                    <li><a href="mailto:{{siteMeta.author.email}}">Send email</a></li>
                    <li><a href="TODO: Feed link">Subscribe</a></li>
                </ul>
            </nav>
        </footer>
  </body>
</html>
